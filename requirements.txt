# Third-party Python dependencies for the AgencyZoom scraper.
# Everything else used by the code (tkinter, csv, pathlib, re, etc.) is
# part of the Python standard library and needs no installation.
#
# Install with:  python -m pip install -r requirements.txt
openpyxl==3.1.5
playwright==1.60.0
